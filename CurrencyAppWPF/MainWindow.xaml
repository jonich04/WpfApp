<Window x:Class="CurrencyAppWPF.MainWindow"
        x:Name="MainWindowName"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CurrencyAppWPF.ViewModels"
        mc:Ignorable="d"
        Title="Курсы валют" Height="600" Width="900"
        WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <DataTemplate x:Key="CurrencyItemTemplate">
            <Border BorderBrush="Gray" BorderThickness="1" CornerRadius="5" Padding="10" Margin="5">
                <StackPanel>
                    <TextBlock FontSize="18" FontWeight="Bold">
                        <Run Text="{Binding CharCode}"/>
                        <Run Text=" - "/>
                        <Run Text="{Binding Name}"/>
                    </TextBlock>
                    <TextBlock Margin="0,5">
                        <Run Text="Курс: "/>
                        <Run Text="{Binding Value}" FontWeight="Bold" Foreground="Green"/>
                        <Run Text=" руб."/>
                    </TextBlock>
                    <TextBlock>
                        <Run Text="Номинал: "/>
                        <Run Text="{Binding Nominal}"/>
                    </TextBlock>
                    <TextBlock FontSize="11" Foreground="Gray">
                        <Run Text="Добавлена: "/>
                        <Run Text="{Binding AddedDate, StringFormat='dd.MM.yyyy HH:mm'}"/>
                    </TextBlock>
                    <Button Content="Удалить" 
                            Background="Red" 
                            Foreground="White"
                            Margin="0,5"
                            Command="{Binding ElementName=MainWindowName, Path=DataContext.DeleteCurrencyCommand}"
                            CommandParameter="{Binding}"/>
                </StackPanel>
            </Border>
        </DataTemplate>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Margin="10" Orientation="Horizontal" HorizontalAlignment="Center">
            <Button Content="Обновить из интернета" 
                    Command="{Binding UpdateFromApiCommand}"
                    Background="Green" 
                    Foreground="White"
                    Padding="15,8"
                    Margin="5"
                    FontSize="14"
                    FontWeight="Bold"/>

            <Button Content="Добавить валюту" 
                    Click="OpenAddCurrencyWindow"
                    Background="Blue" 
                    Foreground="White"
                    Padding="15,8"
                    Margin="5"
                    FontSize="14"
                    FontWeight="Bold"/>

            <Button Content="Настройки" 
                    Click="OpenSettingsWindow"
                    Background="Orange" 
                    Foreground="White"
                    Padding="15,8"
                    Margin="5"
                    FontSize="14"
                    FontWeight="Bold"/>
        </StackPanel>
        <ScrollViewer Grid.Row="1">
            <ItemsControl ItemsSource="{Binding Currencies}" 
                          ItemTemplate="{StaticResource CurrencyItemTemplate}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
            </ItemsControl>
        </ScrollViewer>
        <Border Grid.Row="2" Background="LightGray" Padding="10">
            <StackPanel>
                <TextBlock Text="{Binding LastSessionDate}" FontWeight="Bold" Margin="5"/>
            </StackPanel>
        </Border>
        <Border Grid.RowSpan="3" Background="Black" Opacity="0.5" Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibility}}">
            <TextBlock Text="ЗАГРУЗКА..." 
                       Foreground="White" 
                       FontSize="24" 
                       FontWeight="Bold"
                       VerticalAlignment="Center"
                       HorizontalAlignment="Center"/>
        </Border>
    </Grid>
</Window>