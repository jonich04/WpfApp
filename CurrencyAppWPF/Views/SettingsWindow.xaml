<Window x:Class="CurrencyAppWPF.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Настройки" Height="250" Width="400"
        WindowStartupLocation="CenterOwner">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Способ хранения данных" FontWeight="Bold" FontSize="14" Margin="0,0,0,10"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,10">
            <RadioButton Content="JSON файл" IsChecked="{Binding UseJson}" Margin="0,0,20,0"/>
            <RadioButton Content="SQLite" IsChecked="{Binding UseSqlite}" Margin="0,0,20,0"/>
        </StackPanel>

        <TextBlock Grid.Row="2" Text="{Binding InfoText}" Foreground="Gray" Margin="0,10,0,0"/>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,20">
            <Button Content="Сохранить" Command="{Binding SaveCommand}" Padding="30,8" Margin="5"/>
            <Button Content="Закрыть" Command="{Binding CloseCommand}" Padding="30,8" Margin="5"/>
        </StackPanel>
    </Grid>
</Window>